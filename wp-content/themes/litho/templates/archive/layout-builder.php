<?php
/**
 * The template for displaying the archive from section builder
 *
 * @package Litho
 */

// Exit if accessed directly.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Fires to Load Archive Content from the Section Builder.
 *
 * @since 1.0
 */
do_action( 'theme_archive' );
